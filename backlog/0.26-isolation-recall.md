# 0.26 — isolation + recall

Shipped in this branch. Follow-ups only.

## Done

- 0063: `brain_sessions` writes are not tenant-checked; SELECT stays scoped.
- 0064: `UNIQUE NULLS NOT DISTINCT (name, project_id)` on entities; clone `architecture_decisions` per project.
- jornada start / proyecto switch in `SET LOCAL app.current_project='*'` so RLS cannot lock the writer into the previous tenant.
- faro: `vector_search` honors scope; `include_unscoped` (default false); tag filter does not dump observations into `scope=errors`.
- decreto query is `ts_rank` + `ILIKE` + trigram 0.1, not a 0.2 wall on formatted docs.
- HTTP bind key is `Mcp-Client-Id` + `Mcp-Session-Id`.
- whoami.llm.configured is true only with a provider or base URL.
- doctor warns if `CUBA_HTTP_ADDR` is `:8788` (Cursor OAuth).
- setup check flags HTTP on 8788 and HTTP without `Mcp-Client-Id`.
- recall scopes last session / errors / decisions to the project; errors require `trust=trusted`.
- `memory-industry project backfill <name> [--apply]`.
- v041 isolation tests + e2e decreto count + eval isolation oracles.

## Open (not blockers)

- Restart the live MCP/daemon so 0063/0064 apply; the process still in memory is 0.25 and jornada start still dies on RLS.
- Isolation eval JSONL scores by markers / abstention. Gold observation UUIDs from the live corpus would be tighter — add them only after a read-only harvest, never by mutating production.
- `cuba_proyecto switch` with no open jornada still cannot bind a session (by design). Agents must `cuba_jornada start --project` first.
